server:
  port: 8080

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://你的服务器IP:3306/smart_mall
    username: root
    password: ********

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  type-aliases-package: com.smartmall.entity

  configuration:
    map-underscore-to-camel-case: true